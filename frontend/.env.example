# Copy this file to .env for local overrides, or set this as an environment
# variable in your hosting provider (Vercel/Netlify) for production builds.
# Point it at your deployed Django backend, including the /api suffix.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
